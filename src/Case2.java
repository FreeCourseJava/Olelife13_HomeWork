public class Case2 {
    // ПервоначальнояЯ видела только грустную надпись "java: incompatible types: possible lossy conversion
    // from int to char" и грустила про несовместимость всего живого в этом мире. После второго урока немного
    // попробовала casting.
    public static void main(String[] args) {
 char bookva = 2;
 int chislo = 1;
 int sumBookva = (int)(bookva + chislo);
 System.out.println(sumbookva);

    }
}
